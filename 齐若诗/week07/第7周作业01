【第六周作业01】
1.文本分类（意图识别）与命名实体识别（槽位填充）属于多任务学习中的硬共享关系。它们共享同一个 BERT 编码器提取特征，但在顶层分化为不同的任务头。
对比维度	意图识别	实体识别
特征来源	 CLS经过池化后的向量	BERT 的 token_output
即每个字/词的局部上下文向量
语义层级	捕捉整个句子的全局语义信息	捕捉句子中各个 Token 的局部特征与依赖
损失函数	交叉熵损失	交叉熵损失
在代码实现中，这两个任务的 Loss 分别计算：意图 Loss：衡量句子全局类别预测与真实意图的差异；实体识别 Loss：衡量所有有效 Token（排除 padding）的类别预测与真实标签的差异平均值。

2.在基础模型中，总损失函数被简单定义为：Loss = seq_loss + token_loss 
这种 1:1 的简单相加在实际工程中往往会带来以下三个明显问题，导致模型“训练不平衡”：
（1）量级差异主导梯度：如果实体识别类别繁多或句子较长，初始阶段 token_loss的数值通常会远大于seq_loss。这会导致反向传播时总梯度被 NER 任务主导，意图识别任务沦为“搭便车”角色，收敛极速下降。
（2）收敛速度不一致：文本分类往往比序列标注任务更简单，收敛更快。简单相加会导致：当意图识别已经拟合甚至过拟合时，槽位填充可能还在挣扎。此时强行继续整体训练，会严重损害意图识别的泛化能力。
（3）任务冲突与负迁移：两个任务在特征空间的最优解可能并不完全重合。强行拉平权重容易引发“跷跷板效应”，即为了迎合某一任务的特征分布，反而降低了另一任务的准确率。
3.可以通过以下略来进行干预和优化：
1. 静态加权 :引入人工配置的超参数，通过先验经验或验证集表现来平衡两者的权重。如果 NER 的 Loss 始终主导网络，可以降低其权重。
LOSS = αLseq + (1−α)Ltoken
2. 动态损失加权,让模型在训练过程中自动学习并调整任务权重，引入可学习的噪声参数sigma,自动抑制高噪音任务的 Loss。
3. 引入 Focal Loss 缓解类别极度不均：实体填充任务中通常存在大量的非实体标签（O 标签），这会导致模型被海量简单样本淹没，难以学习到真正有用的实体特征。将 nn.CrossEntropyLoss() 替换为 Focal Loss，可以强迫模型将注意力集中在难以分类的少数重点实体标签上。
4. 差异化学习率 (Differential Learning Rates)：对网络的不同层级应用不同的学习率，避免一刀切。通常可以为共享的 BERT 底层设置一个较小的学习率（如 2e-5）以保护预训练特征，而给顶层的 sequence_classification 和token_classification 设置较大的学习率（如 1e-4），加速任务头的收敛。
